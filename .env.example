# set project's python path
PYTHONPATH=src

# Resource paths for the Shor benchmark pilot configuration.
# Copy to .env and customize if your files live outside the default repo layout.

SHOR_HARDWARE_CONFIG_PATH=data/hardware.toml
SHOR_OPT_CIRCUITS_PATH=data/opt_circuits

# Backward-compatible generic names also work for the Shor benchmark.
# HARDWARE_CONFIG_PATH=data/hardware.toml
# OPT_CIRCUITS_PATH=data/opt_circuits
